[pytest]
testpaths = ssot/tests assets/tests kyc/tests admin/tests payments/tests
addopts = -v --import-mode=importlib
markers =
    integration: marks tests that need external services (firestore emulator, live web)
