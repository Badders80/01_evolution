# 2026-05-20 — Content Repository + Bulk Upload

**Date:** 2026-05-20
**Focus:** Content repository bulk upload system
**Files Changed:** 8 (3 new, 5 modified)
**Location:** Originally `01_evolution/app/` — **moved to `02_website/src/app/admin/` on 2026-05-20**

**Keywords:** bulk-upload, assets, content-repository, smart-naming, auto-tagging, drag-and-drop, horse-selector, HLT-owner-resolution

## Tasks
- [x] Plan bulk upload architecture (3-step intake: horse → context → files)
- [x] Build bulk upload API endpoint (POST /bulk-upload with multi-file + metadata)
- [x] Build smart file naming logic (horse-slug_date_context_seq.ext)
- [x] Build bulk upload UI with drag-and-drop zone
- [x] Build intake form with horse selector + context fields
- [x] Build file preview with auto-renaming, reorder, primary selection
- [x] Wire up API client for bulk upload (assets.bulkUpload method)
- [x] Wire up routes and test build (Next.js + Python syntax verified)
- [x] Fix owner resolution path (owner via HLT, not horse.owner_id)
- [x] Fix breeder example ("Golden Eye Trust" not "Goldeye Trust")
- [x] Create /end skill for session close documentation
- [ ] Integration tests for bulk upload
- [ ] Signed URLs for GCS (currently using public URLs)

## Decisions
- Owner resolved through HLT (horse_microchip → hlt.owner_id → owners), not directly on horse — the horse model has no owner_id field
- Filenames follow pattern: {horse-slug}_{date}_{context-slug}_{seq}.{ext} — e.g. prudentia-nz_2026-05-20_morning-training_001.jpg
- Auto-tags generated from: horse name, owner name (via HLT), trainer name, location, context
- Original filename preserved in `original_file_name` field for audit trail
- Primary image selection via `primary_index` (0-based) in bulk upload
- /end skill writes to project docs/ structure, not generic memory location

## Blockers
- None

## Shipped
Built a complete content repository intake system. New API endpoint (POST /bulk-upload) accepts multiple files with context metadata, generates meaningful filenames, auto-tags from horse/owner/trainer/location, and creates thumbnails. New admin page at /admin/assets/upload provides a 3-step drag-and-drop workflow. Fixed owner resolution to go through HLT instead of a non-existent horse.owner_id field.

## Unfinished
Integration tests for the bulk upload flow, signed URLs for GCS, horse detail page with image gallery.

## Narrative
The session focused on building a content repository that treats images as entity-anchored assets. The key insight was that every image belongs to a horse (identified by microchip), and context like "morning training at Wexford" should be captured at upload time and baked into both the filename and tags. A data model bug was caught — the owner relationship is through the HLT, not the horse record. Also created the /end skill to formalize session close documentation into the project's existing docs/ structure.

## Next
- Horse detail page with image gallery (consuming the content repository)
- HLT document generation workflow
- Integration tests for bulk upload

## Verification
```bash
cd /home/evo/projects/01_evolution/app && npm run build 2>&1 | tail -30
# Result: ✓ Compiled successfully, all 16 pages generated, 0 errors

cd /home/evo/projects/01_evolution/api/assets && python3 -c "import ast; ast.parse(open('routes/bulk_upload.py').read()); print('✓ bulk_upload.py syntax OK')"
# Result: ✓ bulk_upload.py syntax OK
```

---

# 2026-05-20 — Sprint 2: Horse Registration + Upload Workflow

**Date:** 2026-05-20
**Focus:** Horse detail page, upload workflow, build verification
**Files Changed:** 5 (2 created, 3 modified)

**Keywords:** horse-registration, upload, admin, sprint-2, build-fix, typescript, suspense-boundary

## Tasks
- [x] Recreate upload/page.tsx after file corruption ✅
- [x] Add Suspense boundary for useSearchParams (Next.js 16 requirement) ✅
- [x] Create horse detail page with image gallery ✅
- [x] Wire up asset retrieval by entity_type + entity_id ✅
- [x] Fix TypeScript errors (content_type vs mime_type) ✅
- [x] Make horse names clickable (link to detail page) ✅
- [x] Add "Edit Horse" button to detail page ✅
- [x] Add loading skeletons (shimmer effect) ✅
- [x] Add empty states (no horses, no images) ✅
- [x] Verify build passes (22 pages, 0 errors) ✅
- [ ] Test end-to-end with 4 horse URLs (requires backend API running)

## Decisions
- Asset interface uses `content_type` field (not `mime_type`) for consistency with API response
- Upload page wrapped in Suspense boundary to support useSearchParams in Next.js 16
- Horse detail page uses dynamic route `[microchip]` for natural key routing
- Upload flow: horse detail → "Upload Image" button → upload page with pre-filled entity_id

## Blockers
- None

## Shipped
Fixed corrupted upload page (recreated from scratch with full validation), created horse detail page with image gallery and lightbox, wired up asset retrieval API, fixed TypeScript errors, added "Edit Horse" button, added loading skeletons with shimmer effect, verified empty states exist (horses list + image gallery), verified build passes with 22 pages. **Sprint 2 is 100% complete** (23/23 items done).

## Unfinished
End-to-end testing with the 4 horse URLs requires the backend API to be running (SSOT API on :8080, Assets API on :8081).

## Narrative
Today was about finishing Sprint 2's core workflow: register a horse → upload images → view them in a gallery. The upload page got corrupted during a string replacement, requiring a full recreation with proper Suspense boundaries for Next.js 16. The horse detail page came together cleanly, fetching both horse data and assets, then displaying them in a grid with lightbox. Added "Edit Horse" button, loading skeletons with shimmer effects, and verified empty states. Build passes with 22 pages. **Sprint 2 is now complete.** Backend integration testing pending (APIs not running).

## Next
- **Backend integration testing** — Start SSOT + Assets APIs, test with 4 horse URLs
- **Sprint 3** — HLT document generation workflow
- **Auth/KYC** — Firebase + Stripe integration

## Verification```bash
cd /home/evo/evo_01/02_website && npm run build 2>&1 | tail -30
# Result: ✓ Compiled successfully in 3.0s, 22 pages generated, 0 TypeScript errors
# Routes: /admin/horses/new (static), all other admin pages static
```

---

# 2026-05-20 — Persistent Navbar + Horse Registration Flow

**Date:** 2026-05-20
**Focus:** Persistent navbar, horse registration UX, auto-population
**Files Changed:** 3 (1 modified: admin/layout.tsx, new/page.tsx)

**Keywords:** persistent-navbar, horse-registration, loveracing-import, auto-population, ux-improvement, font-styling

## Tasks
- [x] Add persistent website navbar to admin layout ✅
- [x] Offset admin content below navbar (mt-16 spacing) ✅
- [x] Apply Geist Sans font to admin sidebar + content ✅
- [x] Redesign horse registration as 2-step flow ✅
- [x] Step 1: Import from loveracing.nz URL ✅
- [x] Step 2: Verify & complete with auto-populated fields ✅
- [x] Extract name, colour, foaling year from URL slug ✅
- [x] Auto-populate form on successful URL validation ✅
- [x] Verify build passes (22 pages, 0 errors) ✅

## Decisions
- **Loveracing URL is the entry point** — user starts with URL, not manual entry
- **Microchip becomes SSOT after entry** — 15-digit permanent anchor
- **Name extraction from slug** — "Prudentia-NZ-2021" → "Prudentia (NZ)"
- **Functional state updates** — use `setForm(prev => ...)` to avoid stale closures
- **Visual hierarchy** — Step 1 (import) → Step 2 (verify & complete)

## Blockers
- None

## Shipped
Added persistent website navbar to all admin pages with proper spacing. Redesigned horse registration as a 2-step workflow: paste loveracing.nz URL → auto-populate form fields. Extraction logic parses URL slug to fill name, colour, and foaling date automatically. Build passes with 22 pages.

## Unfinished
Production testing of auto-population (dev mode HMR caching may cause visual lag). Backend API integration to replace simulated extraction with actual loveracing.nz scraping.

## Narrative
The session focused on two key UX improvements: making the website navbar persistent across all admin pages (with proper offset spacing), and redesigning the horse registration flow to start with the loveracing.nz URL as the entry point. The auto-population logic extracts data from the URL slug and fills the form automatically, reducing manual data entry. Console logs confirmed extraction and state updates are working correctly.

## Next
- Production build testing (verify auto-population renders correctly)
- Backend API for loveracing.nz scraping
- Sprint 3: HLT document generation```bash
cd /home/evo/evo_01/02_website && npm run build
# Result: ✓ Compiled successfully in 3.1s, 22 pages generated, 0 TypeScript errors
# Routes: /admin/horses/[microchip] (dynamic), /admin/assets/upload (static)
```

## Unfinished
- Integration tests for auth flows (email login, Google login, KYC end-to-end)
- Environment variable setup (.env.local with Firebase config, Stripe keys)
- Firebase Google Auth provider enablement in Firebase Console
- Stripe webhook endpoint registration
- Deploy to Vercel/Firebase Hosting

## Narrative
The morning session focused on making auth "real" — the login page was a stub that unconditionally redirected to /admin. We wired Firebase signInWithEmailAndPassword, added GoogleAuthProvider with a polished SVG button, and created AuthContext to track user, role, and KYC status globally. The admin sidebar got a complete redesign with section headers, a user profile card showing role and KYC badges, and an admin-only Website section for future CMS work. The dashboard now pulls live counts from all five APIs. The KYC verify page was fixed to use the actual user.uid from auth context instead of a hardcoded string. A missing globals.css import was caught during browser preview and fixed. The 02_website project was reviewed and confirmed as a clean integration target — Next.js 16, minimal deps, JSON-driven content, production-ready SEO.

## Next
- Integration testing: email login → admin → KYC verify → Stripe redirect
- Environment setup: create .env.local template with all required variables
- Deploy admin portal to staging
- Begin integration with 02_website (shared auth, API client)

## Verification
```bash
cd /home/evo/projects/01_evolution/app && npm run build 2>&1 | tail -40
# Result: ✓ Compiled successfully in 1840ms
#          Linting and checking validity of types ...
#          Collecting page data ...
#          Generating static pages (18/18)
#          Finalizing page optimization ...
#          Route (app) — 18 pages, 0 errors
```

---

# 2026-05-20 — Session 3: Doc Structure + /end Skill

**Date:** 2026-05-20
**Focus:** Documentation structure, /end skill, 02_website READMEs
**Files Changed:** 18 (14 new, 4 modified)

**Keywords:** docs-structure, end-skill, progress-diary, build-summary-map, readme-mirror, 02-website

## Tasks
- [x] Nuke workspace/ directory (old /done skill's generic memory location)
- [x] Sharpen PROGRESS vs BUILD_SUMMARY boundary (diary vs map)
- [x] Remove "What's Next" and "Architecture Status" from BUILD_SUMMARY (belongs in PROGRESS)
- [x] Add diary/map callouts to both docs
- [x] Create /end skill (writes to docs/, not generic memory)
- [x] Mirror docs structure to 02_website (13 READMEs created)
- [x] Update 01_evolution docs/README.md to match new diary/map language
- [x] Update 02_website docs/README.md and logs/README.md
- [x] Fix broken table at end of BUILD_SUMMARY.md (leftover from old extraction)

## Decisions
- PROGRESS.md = the diary (session log, what's next, architecture status) — updated every session
- BUILD_SUMMARY.md = the map (what the project is, what exists, the rules) — updated only when landscape shifts
- /end skill writes to project docs/ structure, never to generic memory locations
- BUILD_SUMMARY never contains: What's Next, Architecture Status table, Key Files — those live in PROGRESS
- Every folder (outside .xx/_xx) gets a README.md explaining its purpose and structure

## Blockers
- None

## Shipped
Sharpened the documentation boundary between PROGRESS (diary) and BUILD_SUMMARY (map), removing all overlap. Created the /end skill that writes to the project's docs/ structure. Nuked the old workspace/ directory. Mirrored 13 README.md files to 02_website so both projects have the same documentation pattern — every folder explains itself.

## Unfinished
- 02_website still needs content in dna/brand/, dna/content/, dna/conventions/ (READMEs exist but files don't)
- 02_website docs/audit/AUDIT.md doesn't exist yet

## Narrative
This session was about documentation hygiene. The PROGRESS and BUILD_SUMMARY docs had significant overlap — both had "What's Next" sections and architecture status tables. We drew a clear line: PROGRESS is the diary (changes every session), BUILD_SUMMARY is the map (changes only when the landscape shifts). The /end skill was rewritten to enforce this boundary. Then we mirrored the entire README-per-folder pattern from 01_evolution to 02_website, creating 13 new READMEs so every folder in both projects explains its purpose.

## Next
- Horse detail page with image gallery
- HLT document generation workflow
- Integration tests for bulk upload

## Verification
```bash
cd /home/evo/projects/01_evolution/app && npm run build 2>&1 | tail -30
# Result: ✓ Compiled successfully, all 16 pages generated, 0 errors
```